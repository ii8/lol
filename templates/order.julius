
function call(op, order)
{
    var success = function(response)
    {
       $('#order-table').replaceWith(response);
    };

    var fail = function(status, msg)
    {
        alert(status + ": " + msg);
    };

    ajax("/ajax/order/" + op + "/" + order, null, success, fail);
}
